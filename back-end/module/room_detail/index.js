const { update } = require("../auth/model");
const detailModel = require("./model")

const handlers = {
    async addDetail(req,res,next){
        throw new Error("Not yet support");
    },
    async readDetail(req,res,next){
        throw new Error("Not yet support");
    },
    async updateDetail(req,res,next){
        throw new Error("Not yet support");
    },
    async deleteDetail(req,res,next){
        throw new Error("Not yet support");
    }
}

module.exports = handlers